//Cursor hecho en Godot
extends Node

func _ready():
	var cursor_texture = load("res://frisbee cursor tr.png")  # localizacion del archivo
	var hotspot = Vector2(32, 32)  # pixeles de la imagen
	Input.set_custom_mouse_cursor(cursor_texture, Input.CURSOR_ARROW, hotspot) #cursor
